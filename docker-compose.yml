# this file is automatically generated. DO NOT EDIT
version: '3.8'
services:
  gitbundle:
    container_name: gitbundle
    image: gitbundle/gitbundle
    volumes:
    - gitbundle-data:/data
    - /etc/localtime:/etc/localtime:ro
    - /etc/hosts:/etc/hosts:ro
    environment:
    - APP_NAME=${APP_NAME:-"GitBundle"}
    - RUN_USER=${RUN_USER:-git}
    - DOMAIN=${DOMAIN:-example.gitbundle.com}
    - SSH_DOMAIN=${SSH_DOMAIN:-example.gitbundle.com}
    - HTTP_PORT=${HTTP_PORT:-4000}
    - ROOT_URL=${ROOT_URL:-}
    - DISABLE_SSH=${DISABLE_SSH:-false}
    - SSH_PORT=${SSH_PORT:-22}
    - LFS_START_SERVER=${LFS_START_SERVER:-true}
    - DB_TYPE=${DB_TYPE:-postgres}
    - DB_HOST=${DB_HOST:-db:5432}
    - DB_NAME=${DB_NAME:-gitbundle}
    - DB_USER=${DB_USER:-postgres}
    - DB_PASSWD=${DB_PASSWD:-postgres}
    - DB_SCHEMA=${DB_SCHEMA:-public}
    - SSL_MODE=${SSL_MODE:-disable}
    - LOG_SQL=${LOG_SQL:-false}
    - INSTALL_LOCK=${INSTALL_LOCK:-true}
    - DISABLE_REGISTRATION=${DISABLE_REGISTRATION:-false}
    - REQUIRE_SIGNIN_VIEW=${REQUIRE_SIGNIN_VIEW:-false}
    - SECRET_KEY=${SECRET_KEY:-}
    - NSQD_CLUSTER_TCP_ADDR=${NSQD_CLUSTER_TCP_ADDR:-}
    - REDIS_CONNECTION=${REDIS_CONNECTION:-}
    depends_on:
    - db
    - nsq
    - redis-server
    restart: always
    networks:
    - gitbundle-private
  bundle-deployments:
    container_name: bundle-deployments
    image: gitbundle/bundle-deployments
    volumes:
    - /etc/localtime:/etc/localtime:ro
    - /etc/hosts:/etc/hosts:ro
    environment:
    - BUNDLE_DEPLOYMENTS_GITBUNDLE_SERVER=${BUNDLE_DEPLOYMENTS_GITBUNDLE_SERVER:-}
    - BUNDLE_DEPLOYMENTS_GITBUNDLE_SKIP_VERIFY=${BUNDLE_DEPLOYMENTS_GITBUNDLE_SKIP_VERIFY:-false}
    - BUNDLE_DEPLOYMENTS_DATABASE_DRIVER=${BUNDLE_DEPLOYMENTS_DATABASE_DRIVER:-}
    - BUNDLE_DEPLOYMENTS_DATABASE_HOST=${BUNDLE_DEPLOYMENTS_DATABASE_HOST:-}
    - BUNDLE_DEPLOYMENTS_DATABASE_NAME=${BUNDLE_DEPLOYMENTS_DATABASE_NAME:-}
    - BUNDLE_DEPLOYMENTS_DATABASE_USER=${BUNDLE_DEPLOYMENTS_DATABASE_USER:-}
    - BUNDLE_DEPLOYMENTS_DATABASE_PASSWD=${BUNDLE_DEPLOYMENTS_DATABASE_PASSWD:-}
    - BUNDLE_DEPLOYMENTS_LOGS_LEVEL=trace
    - BUNDLE_DEPLOYMENTS_LOGS_COLOR=true
    - BUNDLE_DEPLOYMENTS_LOGS_TEXT=true
    - BUNDLE_DEPLOYMENTS_LOGS_CALLER=true
    - BUNDLE_DEPLOYMENTS_REDIS_CONNECTION=${BUNDLE_DEPLOYMENTS_REDIS_CONNECTION:-}
    - BUNDLE_DEPLOYMENTS_NSQ_CLUSTER_TCP_ADDR=${BUNDLE_DEPLOYMENTS_NSQ_CLUSTER_TCP_ADDR:-}
    - BUNDLE_DEPLOYMENTS_NSQ_AUTH_SECRET=${BUNDLE_DEPLOYMENTS_NSQ_AUTH_SECRET:-}
    - BUNDLE_DEPLOYMENTS_PLUGIN_NAME=${BUNDLE_DEPLOYMENTS_PLUGIN_NAME:-}
    - BUNDLE_DEPLOYMENTS_PLUGIN_TOKEN=${BUNDLE_DEPLOYMENTS_PLUGIN_TOKEN:-}
    depends_on:
    - db
    - gitbundle
    - redis-server
    - nsq
    restart: always
    networks:
    - gitbundle-private
  bundle-pods:
    container_name: bundle-pods
    image: gitbundle/bundle-pods
    volumes:
    - /etc/localtime:/etc/localtime:ro
    - /etc/hosts:/etc/hosts:ro
    environment:
    - BUNDLE_PODS_GITBUNDLE_SERVER=${BUNDLE_PODS_GITBUNDLE_SERVER:-}
    - BUNDLE_PODS_GITBUNDLE_SKIP_VERIFY=${BUNDLE_PODS_GITBUNDLE_SKIP_VERIFY:-false}
    - BUNDLE_PODS_DATABASE_DRIVER=${BUNDLE_PODS_DATABASE_DRIVER:-}
    - BUNDLE_PODS_DATABASE_HOST=${BUNDLE_PODS_DATABASE_HOST:-}
    - BUNDLE_PODS_DATABASE_NAME=${BUNDLE_PODS_DATABASE_NAME:-}
    - BUNDLE_PODS_DATABASE_USER=${BUNDLE_PODS_DATABASE_USER:-}
    - BUNDLE_PODS_DATABASE_PASSWD=${BUNDLE_PODS_DATABASE_PASSWD:-}
    - BUNDLE_PODS_LOGS_LEVEL=trace
    - BUNDLE_PODS_LOGS_COLOR=true
    - BUNDLE_PODS_LOGS_TEXT=true
    - BUNDLE_PODS_LOGS_CALLER=true
    - BUNDLE_PODS_REDIS_CONNECTION=${BUNDLE_PODS_REDIS_CONNECTION:-}
    - BUNDLE_PODS_NSQ_CLUSTER_TCP_ADDR=${BUNDLE_PODS_NSQ_CLUSTER_TCP_ADDR:-}
    - BUNDLE_PODS_NSQ_AUTH_SECRET=${BUNDLE_PODS_NSQ_AUTH_SECRET:-}
    - BUNDLE_PODS_PLUGIN_NAME=${BUNDLE_PODS_PLUGIN_NAME:-}
    - BUNDLE_PODS_PLUGIN_TOKEN=${BUNDLE_PODS_PLUGIN_TOKEN:-}
    depends_on:
    - db
    - gitbundle
    - redis-server
    - nsq
    restart: always
    networks:
    - gitbundle-private
  bundle-metrics:
    container_name: bundle-metrics
    image: gitbundle/bundle-metrics
    volumes:
    - /etc/localtime:/etc/localtime:ro
    - /etc/hosts:/etc/hosts:ro
    environment:
    - BUNDLE_METRICS_GITBUNDLE_SERVER=${BUNDLE_METRICS_GITBUNDLE_SERVER:-}
    - BUNDLE_METRICS_GITBUNDLE_SKIP_VERIFY=${BUNDLE_METRICS_GITBUNDLE_SKIP_VERIFY:-false}
    - BUNDLE_METRICS_DATABASE_DRIVER=${BUNDLE_METRICS_DATABASE_DRIVER:-}
    - BUNDLE_METRICS_DATABASE_HOST=${BUNDLE_METRICS_DATABASE_HOST:-}
    - BUNDLE_METRICS_DATABASE_NAME=${BUNDLE_METRICS_DATABASE_NAME:-}
    - BUNDLE_METRICS_DATABASE_USER=${BUNDLE_METRICS_DATABASE_USER:-}
    - BUNDLE_METRICS_DATABASE_PASSWD=${BUNDLE_METRICS_DATABASE_PASSWD:-}
    - BUNDLE_METRICS_LOGS_LEVEL=trace
    - BUNDLE_METRICS_LOGS_COLOR=true
    - BUNDLE_METRICS_LOGS_TEXT=true
    - BUNDLE_METRICS_LOGS_CALLER=true
    - BUNDLE_METRICS_REDIS_CONNECTION=${BUNDLE_METRICS_REDIS_CONNECTION:-}
    - BUNDLE_METRICS_NSQ_CLUSTER_TCP_ADDR=${BUNDLE_METRICS_NSQ_CLUSTER_TCP_ADDR:-}
    - BUNDLE_METRICS_NSQ_AUTH_SECRET=${BUNDLE_METRICS_NSQ_AUTH_SECRET:-}
    - BUNDLE_METRICS_PLUGIN_NAME=${BUNDLE_METRICS_PLUGIN_NAME:-}
    - BUNDLE_METRICS_PLUGIN_TOKEN=${BUNDLE_METRICS_PLUGIN_TOKEN:-}
    depends_on:
    - db
    - gitbundle
    - redis-server
    - nsq
    restart: always
    networks:
    - gitbundle-private
  bundle-builds:
    container_name: bundle-builds
    image: gitbundle/bundle-builds
    ports:
    - 8080:8080
    environment:
    - BUNDLE_BUILDS_SERVER_HOST=${BUNDLE_BUILDS_SERVER_HOST:-localhost:8080}
    - BUNDLE_BUILDS_SERVER_PORT=${BUNDLE_BUILDS_SERVER_PORT:-:8080}
    - BUNDLE_BUILDS_SERVER_PROTO=${BUNDLE_BUILDS_SERVER_PROTO:-http}
    - BUNDLE_BUILDS_SERVER_PROXY_HOST=${BUNDLE_BUILDS_SERVER_PROXY_HOST:-bundle-builds:8080}
    - BUNDLE_BUILDS_SERVER_PROXY_PROTO=${BUNDLE_BUILDS_SERVER_PROXY_PROTO:-http}
    - BUNDLE_BUILDS_RPC_SECRET=${BUNDLE_BUILDS_RPC_SECRET:-}
    - BUNDLE_BUILDS_COOKIE_SECRET=${BUNDLE_BUILDS_COOKIE_SECRET:-}
    - BUNDLE_BUILDS_COOKIE_TIMEOUT=${BUNDLE_BUILDS_COOKIE_TIMEOUT:-720h}
    - BUNDLE_BUILDS_GITBUNDLE_SERVER=${BUNDLE_BUILDS_GITBUNDLE_SERVER:-https://example.gitbundle.com}
    - BUNDLE_BUILDS_GITBUNDLE_DEBUG=${BUNDLE_BUILDS_GITBUNDLE_DEBUG:-false}
    - BUNDLE_BUILDS_GITBUNDLE_SKIP_VERIFY=${BUNDLE_BUILDS_GITBUNDLE_SKIP_VERIFY:-true}
    - BUNDLE_BUILDS_DATABASE_DRIVER=${BUNDLE_BUILDS_DATABASE_DRIVER:-postgres}
    - BUNDLE_BUILDS_DATABASE_DATASOURCE=${BUNDLE_BUILDS_DATABASE_DATASOURCE:-postgres://postgres:postgres@db:5432/bundle-builds?sslmode=disable}
    - BUNDLE_BUILDS_DATABASE_SECRET=${BUNDLE_BUILDS_DATABASE_SECRET:-}
    - BUNDLE_BUILDS_REDIS_CONNECTION=${BUNDLE_BUILDS_REDIS_CONNECTION:-redis://redis-server:6379}
    - BUNDLE_BUILDS_WEBHOOK_ENDPOINT=${BUNDLE_BUILDS_WEBHOOK_ENDPOINT:-}
    - BUNDLE_BUILDS_LOGS_TRACE=${BUNDLE_BUILDS_LOGS_TRACE:-true}
    - BUNDLE_BUILDS_LOGS_COLOR=${BUNDLE_BUILDS_LOGS_COLOR:-true}
    - BUNDLE_BUILDS_LOGS_TEXT=${BUNDLE_BUILDS_LOGS_TEXT:-true}
    - BUNDLE_BUILDS_ARTEFACT_DOCKER_BASE_IMAGE=${BUNDLE_BUILDS_ARTEFACT_DOCKER_BASE_IMAGE:-gitbundle/bundle-docker}
    - BUNDLE_BUILDS_ARTEFACT_DOCKER_REGISTRY_HOST=${BUNDLE_BUILDS_ARTEFACT_DOCKER_REGISTRY_HOST:-}
    - BUNDLE_BUILDS_ARTEFACT_DEBUG_BUILD=${BUNDLE_BUILDS_ARTEFACT_DEBUG_BUILD:-false}
    - BUNDLE_BUILDS_ARTEFACT_DUMP_BUILD=${BUNDLE_BUILDS_ARTEFACT_DUMP_BUILD:-false}
    - BUNDLE_BUILDS_NSQ_CLUSTER_TCP_ADDR=${BUNDLE_BUILDS_NSQ_CLUSTER_TCP_ADDR:-}
    - BUNDLE_BUILDS_NSQ_AUTH_SECRET=${BUNDLE_BUILDS_NSQ_AUTH_SECRET:-}
    - BUNDLE_BUILDS_PLUGIN_TOKEN=${BUNDLE_BUILDS_PLUGIN_TOKEN:-}
    depends_on:
    - db
    - redis-server
    - gitbundle
    - nsq
    restart: always
    networks:
    - gitbundle-private
  bundle-runner-amd64:
    container_name: bundle-runner-amd64
    image: gitbundle/bundle-builds-runner-docker
    platform: linux/amd64
    volumes:
    - /var/run/docker.sock:/var/run/docker.sock
    - /etc/localtime:/etc/localtime:ro
    - /etc/hosts:/etc/hosts:ro
    environment:
    - BUNDLE_BUILDS_TRACE=${BUNDLE_BUILDS_TRACE:-true}
    - BUNDLE_BUILDS_RPC_HOST=${BUNDLE_BUILDS_RPC_HOST:-bundle-builds:8080}
    - BUNDLE_BUILDS_RPC_PROTO=${BUNDLE_BUILDS_RPC_PROTO:-http}
    - BUNDLE_BUILDS_RPC_SECRET=${BUNDLE_BUILDS_RPC_SECRET:-}
    - BUNDLE_BUILDS_RPC_DUMP_HTTP=${BUNDLE_BUILDS_RPC_DUMP_HTTP:-false}
    - BUNDLE_BUILDS_RPC_DUMP_HTTP_BODY=${BUNDLE_BUILDS_RPC_DUMP_HTTP_BODY:-false}
    - BUNDLE_BUILDS_TMATE_ENABLED=${BUNDLE_BUILDS_TMATE_ENABLED:-true}
    - BUNDLE_BUILDS_RUNNER_CLONE_IMAGE=${BUNDLE_BUILDS_RUNNER_CLONE_IMAGE:-gitbundle/bundle-git}
    - BUNDLE_BUILDS_RUNNER_CAPACITY=${BUNDLE_BUILDS_RUNNER_CAPACITY:-2}
    - BUNDLE_BUILDS_DOCKER_STREAM_PULL=${BUNDLE_BUILDS_DOCKER_STREAM_PULL:-true}
    - BUNDLE_BUILDS_PRIVILEGED_IMAGES=${BUNDLE_BUILDS_PRIVILEGED_IMAGES:-}
    depends_on:
    - bundle-builds
    restart: always
    networks:
    - gitbundle-private
  haproxy:
    container_name: haproxy
    image: haproxy:lts-alpine
    ports:
    - '22:22'
    - 80:80
    - 443:443
    volumes:
    - ./haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro
    - ./certs:/certs:ro
    - /etc/localtime:/etc/localtime:ro
    - /etc/hosts:/etc/hosts:ro
    depends_on:
    - gitbundle
    restart: always
    networks:
    - gitbundle-private
  redis-server:
    container_name: redis-server
    image: redis:alpine
    ports:
    - 6379:6379
    volumes:
    - /etc/localtime:/etc/localtime:ro
    - /etc/hosts:/etc/hosts:ro
    restart: always
    networks:
    - gitbundle-private
  nsq:
    container_name: nsq
    image: nsqio/nsq
    ports:
    - 4160:4160
    - 4161:4161
    - 4150:4150
    - 4151:4151
    - 4152:4152
    - 4171:4171
    volumes:
    - nsq-data:/data
    - ./nsq.sh:/workspace/nsq.sh
    - /etc/localtime:/etc/localtime:ro
    - /etc/hosts:/etc/hosts:ro
    command: /workspace/nsq.sh
    networks:
    - gitbundle-private
  db:
    container_name: db
    image: postgres:alpine
    ports:
    - 5432:5432
    volumes:
    - db-data:/var/lib/postgresql/data:rw
    - /etc/localtime:/etc/localtime:ro
    - /etc/hosts:/etc/hosts:ro
    environment:
    - POSTGRES_USER=postgres
    - POSTGRES_PASSWORD=postgres
    healthcheck:
      test:
      - CMD-SHELL
      - pg_isready -U postgres
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
    - gitbundle-private
volumes:
  gitbundle-data:
    driver: local
    driver_opts:
      type: none
      device: $PWD/data/gitbundle
      o: bind
  nsq-data:
    driver: local
    driver_opts:
      type: none
      device: $PWD/data/nsq
      o: bind
  db-data:
    driver: local
    driver_opts:
      type: none
      device: $PWD/data/postgres
      o: bind
networks:
  gitbundle-private:
    driver: bridge
